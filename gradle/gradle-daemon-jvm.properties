# Gradle 8.8+ reads this file to pick the JVM used for the build daemon.
# NeoForge ModDevGradle for Minecraft 1.21.1 needs a JDK that Gradle 8.8 can host (17-22);
# the compiled classes still target Java 21 through java.toolchain below.
toolchainVersion=21
